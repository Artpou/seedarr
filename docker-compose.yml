version: "3.8"

# Note: Database is now SQLite-based (file-based) and doesn't require a container
services:
  redis:
    container_name: cache
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redis:/data

volumes:
  redis:
    driver: local
